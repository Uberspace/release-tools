---
summary: We reach *MVP* state 🎉 - still a new *reno* release would be nice (until now there's no ``0.19.12`` version released, so we keep using *master* from now on).
added:
  - 👷 🐳 Add ``.gitlab-ci.yaml`` to build the Docker image from *master*.
